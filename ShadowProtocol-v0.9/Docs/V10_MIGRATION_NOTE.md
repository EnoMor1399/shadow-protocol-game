# Shadow Protocol v1.0 migration

The v1.0 vertical slice is being created as a versioned sibling directory so the validated v0.9 snapshot remains unchanged. Production changes for v1.0 live under `ShadowProtocol-v1.0/`.
